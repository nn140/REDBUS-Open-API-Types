/**
 * @name BusDecks
 * @description
 * Bus deck types as an `enum` object.
 * - `2` = `BusDecks.DOUBLE`
 * - `1` = `BusDecks.SINGLE`
 *
 * @public
 * @enum BusDecks
 * @module REDBUS
 */
export enum BusDecks {
    DOUBLE = 2,
    SINGLE = 1
}